<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="BaseAppTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- 状态View文本样式：其它配置见global_status_view_config -->
    <style name="style_simple_status_view_text">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">30dp</item>
        <item name="android:textColor">#333333</item>
        <item name="android:textSize">30sp</item>
    </style>

    <!-- 配置全局的actionBar背景，其它配置见global_action_bar_config -->
    <color name="action_bar_bg_color">#80FF0000</color>
    <color name="action_bar_title_color">#0000FF</color>
</resources>
